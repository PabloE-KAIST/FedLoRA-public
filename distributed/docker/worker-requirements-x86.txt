# Worker container pip requirements — x86_64 server variant.
#
# Derived from worker-requirements.txt with version pins relaxed for
# Python 3.11 (official pytorch/pytorch Docker images use conda 3.11).
# numpy<1.23 and scipy==1.7.3 do not have Python 3.11 wheels.

# ZMQ transport
pyzmq==25.1.2

# YAML
PyYAML>=6.0

# Hugging Face stack
transformers==4.43.1
tokenizers==0.19.1
datasets==2.20.0
accelerate==0.33.0
peft==0.4.0
sentencepiece==0.1.99
safetensors>=0.4.0
huggingface-hub==0.35.3

# FederatedScope deps (relaxed for Python 3.11)
numpy<2.0
joblib
threadpoolctl
scikit-learn>=1.0.2
scipy>=1.7.3
pandas
fvcore
iopath
tensorboardX
Pympler
protobuf==3.19.4
grpcio>=1.45.0
dill
yacs==0.1.8

# Transitive deps
regex
filelock
packaging
requests
fsspec<=2024.6.1
pyarrow>=15.0.0
multiprocess
xxhash
psutil
aiohttp
pytz
python-dateutil
typing-extensions
tabulate
portalocker

# Misc
tqdm
